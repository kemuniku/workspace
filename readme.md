つかいかた

1. cloneする
2. `.env`ファイルを作る
3. `.env`ファイルに`REVEL_SESSION`と`CPLIB_PATH`、`NIM_ACL_PATH`を書き込む
4. `sudo ln -s /絶対パス/への/makecontest.py /usr/local/bin/makecontest`とかでシンボリックリンクとかを作る。
5. `keybindings.json`でtaskを呼べるようにすると便利。